{{#contains self 'content'}}

  <h3 class="item__sub-heading">Content</h3>

  <p>This mixin allows extra content to be passed (through the <code>@content</code> directive).{{#isnt self.content ''}} Role: {{self.content }}{{/isnt}}</p>

{{/contains}}